The Dynamic Chatbot is an AI-powered application that can:

Answer user questions intelligently. Generate images from text prompts. Work dynamically using APIs instead of static responses
It is built using FastAPI (backend) and Python, making it fast, lightweight, and easy to extend. What This Chatbot Does Understands user queries using AI models
Generates smart responses instead of fixed replies Creates images from text prompts using OpenAI DALL·E Runs as a REST API that you can test in a browser or Postman.
